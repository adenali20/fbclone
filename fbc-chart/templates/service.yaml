apiVersion: v1
kind: Service
metadata:
  name: {{ include "fbc-chart.fullname" . }}
  labels:
    {{- include "fbc-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fbc-chart.selectorLabels" . | nindent 4 }}
