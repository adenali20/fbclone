apiVersion: v1
kind: Service
metadata:
  name: {{ include "fbc-clone.fullname" . }}
  labels:
    {{- include "fbc-clone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fbc-clone.selectorLabels" . | nindent 4 }}
